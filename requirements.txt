# Core memory system
memo-clover @ git+https://github.com/Qizhan7/MemoClover.git

# HTTP mode (memo-clover --http) + Dashboard
uvicorn>=0.34.0
starlette>=0.27.0
fastapi>=0.115.0

# Dashboard (scheduled task YAML parsing)
pyyaml>=6.0

# System status (imprint-utils)
psutil>=6.0.0
